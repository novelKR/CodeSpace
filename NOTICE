CodeSpace
Copyright 2026 novelKR

This product is licensed under the Apache License, Version 2.0.
See the LICENSE file for the full license text.

This product uses rmcp (Rust SDK for the Model Context Protocol),
which is licensed under the Apache License, Version 2.0.

  Project: https://github.com/modelcontextprotocol/rust-sdk
  Intended reuse: MCP server, tool macros, stdio transport, and
  Streamable HTTP server (`StreamableHttpService`) from the `rmcp` crate.

This product reuses portions of OpenAI Codex, which is licensed
under the Apache License, Version 2.0.

  Project: https://github.com/openai/codex
  Reuse: the pinned Rust crate `codex-apply-patch` (parse, verify, and
  apply) via git submodule `third_party/codex` at
  `6b9826e3aa83b1a5947db50f4332cb9c65f1b340` (`rust-v0.154.0`).
  The pin is recorded in docs/upstream-lock.md.
  File-copy vendoring of a single crate is not used because the crate
  depends on other Codex workspace crates (including
  `codex-exec-server`, path utilities, and tree-sitter bindings).
  The adapter lives in `crates/patch` and calls the library in-process.
  Isolated `crates/codex-runtime` (`codespace-codex-runtime`) reuses
  `codex-process-hardening` and `codex-uds` for the opt-in Unix-socket
  worker. `codespace-patch` also calls `pre_main_hardening()`.

Codex copyright: OpenAI.
Codex license: Apache License 2.0.

rmcp copyright: Model Context Protocol Rust SDK authors.
rmcp license: Apache License 2.0.

Any later reuse of Codex or rmcp source, tests, or documentation will
keep the Apache-2.0 notices required by those licenses. The Codex pin
is a deployment pin for the W06 parity subset; the full upstream suite
is not claimed.
